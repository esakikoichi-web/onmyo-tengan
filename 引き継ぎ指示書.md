# 易経サイト 引き継ぎ指示書（青龍孔一）★2026-08-27更新（GOGI_YAO全64卦完了）

新しいチャットに、この `引き継ぎ指示書.md` と `index.html` をアップして「指示書に沿って続けて」と言えば継続できます（底本xlsxは(A)が完了したため通常は不要。語義の見直し・追加調整をする時のみ再アップ）。

---

## 0. 現在地（何をどこまで）

サイト＝`index.html`（易経「銀河」アプリ。Cloudflare Workers: eki.esaki-koichi.workers.dev にデプロイ）。

### (A) 語義機能＝「各爻の単語の意味」★全64卦完了
- 各卦・各爻の一言変換の下に「**＋単語の意味**」の折り込みを出す機能。**案2＝爻ごと**（その爻に出てくる語だけ）で確定。
- データ＝JS内 `window.__GOGI_YAO = { "卦番号": [ [卦辞の語], [初爻の語], … [上爻の語] ] }`。各爻は `[["語（よみ）","意味"], …]`。7要素（0=卦辞,1=初,2=二,3=三,4=四,5=五,6=上）。語が無い爻は `[]`。
- 表示関数 `window.gogiBoxYao(n, idx)`。銀河パネル＝各爻の hitoBox 直後、一言タブ＝各行の直後に差し込み済み。
- **全64卦入力済み**（1〜64すべて）。底本 `六十四卦個別.xlsx` の卦辞・爻辞に実在する語から拾い、読み仮名も底本準拠、意味は古典字義に沿って簡潔にまとめた（6バッチに分けて作成、それぞれ node --check とJSON検証済みでコミット・プッシュ済み）。
- ★次にやるなら：孔一さんが実際にサイトで表示を確認し、語選び・粒度・訳文のトーンを卦ごとに微調整（気になった卦を指定してもらえば直す）。または(B)の一言変換の見直しへ進む。

### (B) 一言変換の「淡泊な卦」入れ直し（Aと並行の別作業）
- 47沢水困・九五は改稿済み。それ以外は基本さわらない。
- ★重要な教訓：機械で「短い＝薄い」と判定して**本人の完成品を蒸し返すミスを何度もした**。短い＝悪いではない（41損・48井・水風井などは本人が狙って言い切った完成品）。**Claude判断で「薄い」と決めつけない**。直すかどうかは必ず孔一さんが指定。

### (B) 一言変換の「淡泊な卦」入れ直し（Aと並行の別作業）
- 47沢水困・九五は改稿済み。それ以外は基本さわらない。
- ★重要な教訓：機械で「短い＝薄い」と判定して**本人の完成品を蒸し返すミスを何度もした**。短い＝悪いではない（41損・48井・水風井などは本人が狙って言い切った完成品）。**Claude判断で「薄い」と決めつけない**。直すかどうかは必ず孔一さんが指定。

---

## 1. 絶対ルール（一言変換を作る/直すとき）★最重要

1. **本人（孔一さん）の言葉が必ず先。** Claudeが先に一言を書くのは最大の違反。
   手順：**下ごしらえ（原文＋語義）提示 → 芯を待つ → A/B/C → 選択 → 保存**。
2. **A/B/C**：A＝原文の格を残す／B＝切り口を変える／C＝実務・鑑定トーン（★本命。経営者・営業マンに刺さる平易な実務語。古語比喩は残し現代実務語に落とす）。
   - ほぼ必ずC。「c具体的に」→C′。「c具体ok」でC′確定、「短め」で短縮確定。**本人のOKが出るまで確定扱いしない**。
3. **読み仮名は底本（`六十四卦個別.xlsx`「六十四卦」シート）準拠。** 独自の当て字禁止。
4. 語義(A)も同じ精神：底本に実在する語・読みを使い、字義は古典に忠実に。

---

## 2. 保存・実装の技術手順

作業ファイル `/home/claude/work/index.html`（無ければ `cp /mnt/user-data/outputs/index.html /home/claude/work/`）。底本もworkへコピー。**編集は必ずアンカー置換＋一意チェック、保存後に検証**。

### (A) 語義データを1卦追加する（GOGI_YAO）
`window.__GOGI_YAO = {` の直後（`"1": [` の前）に卦を1件 str_replace で追記が安全。形式：
```
"NN": [
  [ ["語（よみ）","意味"], ... ],   // 卦辞
  [ ... ],                          // 初爻
  ... 全7要素（卦辞+6爻）。語が無い爻は [] 。
],
```
追記後に下記JSON検証を通すこと（`json.loads` で `__GOGI_YAO` 全体がパースできる）。底本から語を拾う手順は §3。

### (B) 一言(RECOVER)の1爻を差し替える
```python
path='index.html'; src=open(path,encoding='utf-8').read()
old='九五\u3000＜今の該当爻の全文＞'      # 爻名+\u3000+本文
new='九五\u3000＜確定した新文＞'
assert '"' not in new and '\\n' not in new and 'private' not in new
assert src.count(old)==1
src=src.replace(old,new,1); open(path,'w',encoding='utf-8').write(src)
```
格納形式：1値＝`卦辞\n初X　…\n…\n上X　…`（爻行＝爻名+\u3000+本文）。卦により卦辞と初爻の間に空行あり（例47）。空行は表示側で無視。

### 保存後の検証（毎回・必須）
```python
import json; src=open('index.html',encoding='utf-8').read()
i=src.index('var RECOVER'); b=src.index('{',i); d=0
for j in range(b,len(src)):
    c=src[j]; d+=(c=='{')-(c=='}')
    if d==0 and c=='}': end=j; break
assert len(json.loads(src[b:end+1]))==64
gi=src.index('window.__GOGI_YAO = {'); gb=src.index('{',gi); d=0
for j in range(gb,len(src)):
    c=src[j]; d+=(c=='{')-(c=='}')
    if d==0 and c=='}': ge=j; break
json.loads(src[gb:ge+1]); print("OK")
```
＋ `<script>` 開閉数が一致（現状 8/8）＋ 触った `<script>` を取り出して `node --check`。最後に `cp … /mnt/user-data/outputs/index.html` して `present_files`。

### 底本(xlsx)から爻の原文＋語を読む（§3で使用）
```python
import openpyxl,re
wb=openpyxl.load_workbook('六十四卦個別.xlsx', data_only=True); ws=wb['六十四卦']
raw=[(r[0],r[2]) for r in ws.iter_rows(values_only=True)]  # A列=見出し/記号, C列=書き下し(直前行にひらがなルビ)
# "NN.卦名" 見出し行から次見出しまで。C列で漢字を含み 。or、 の行=書き下し本文。ひらがなのみ=ルビ。
```

---

## 3. 語義バッチの進め方（Aは完了・見直し用に残す）
1. 底本から対象卦の卦辞・全爻を読む（上記コード）。
2. 各爻に出てくる**特徴的な語（古語・専門語）**を選ぶ。1爻2〜4語目安。語が無い爻は `[]`。
3. 読み仮名は底本ルビ準拠。意味は古典字義で簡潔に（既存の1/33/47/48を手本に文体・粒度を合わせる）。
4. `__GOGI_YAO` に追記/差し替え → JSON検証 → node --check → コミット・プッシュ。
5. 全64卦は完了済み。特定の卦の語選び・粒度を見直したい場合はこの手順で該当卦のみ差し替える。孔一さんが卦指定。

---

## 4. 実装済みUI（さわると壊れやすいので把握用）
- タブ：銀河/占筮/検索/断易/鑑定録/八宮/練習/**一言**/**象意**（9個）
- 「一言」タブ：全64卦を➕折り込み一覧。各爻の下に語義（gogiBoxYao）
- 検索カード：➕一言変換
- 銀河パネル：卦辞・各爻の下に➕一言変換、その各爻下に➕単語の意味（gogiBoxYao）
- 「象意」タブ：八卦の基本象意（説卦伝ベース、8カード）
- file://対策：内蔵RECOVERを `window.__HITO_BASE` で公開、表示側は `__hitoStore()` で「内蔵＋localStorage」合成
- 折り込みトグルはすべて `e.stopPropagation()` 済み（カードのタップ遷移と非干渉）

---

## 5. デプロイ
`index.html` を Cloudflare Workers（eki.esaki-koichi.workers.dev）へ。反映確認は本番URLで（localStorageは端末×URLごと。file://直開きでも内蔵データで表示は出る）。

---

## 6. 次チャットでの最初の一言（テンプレ）
- 語義（GOGI_YAO）を見直す：「◯◯卦の語義を見直したい」（底本xlsxが必要ならその時アップ）
- 一言を直す：「◯◯卦の△爻を直したい。原文と語義出して」（→下ごしらえ→芯待ち→A/B/C→保存）
